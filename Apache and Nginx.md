# Intoduction to Apache & Nginx.

Before diving into what apache and nginx is lets take a recap of what web server is. Imagine we open our browser and type `www.google.com`. Our browser is making a request like " Give me this page please". A web server is the software that answers this request, finds the right page and sends it back to our browser.
